# Solstice Circuit

Solstice Circuit is a small browser puzzle game for the DEV June Solstice Game Jam.

The player moves a seeker through a sunlit board, charging binary-coded cipher obelisks before entering the solstice ring. Sunlit movement drains daylight, shadow wells recover it, and the gate only opens when every cipher bit is charged.

## Run

Open `index.html` in a browser.

No build step is required.

For recording, open `index.html?demo=1&recording=1` to auto-start the first-ring demo in a clean 16:9 view.

Submission package:

- `cover.png`
- `solstice-circuit-demo.gif`
- `solstice-circuit-demo.mp4`
- `solstice-circuit-submission.zip`
- `dev-submission-draft.md`
- `dev-quick-submit-fields.md`
- `submission-checklist.md`

## Controls

- Arrow keys or WASD to move
- On touch devices, use the directional buttons
- Reset button to restart
- Hint button to get a short next-target suggestion
- Demo button to auto-play the first ring for recording

## Challenge Fit

The game interprets the solstice as a turning point: the longest day is a limited resource, and the player has to decide when to spend light and when to step into shadow. The binary obelisks add a small code-breaking nod for the challenge's Alan Turing category without claiming any external AI tooling.

## Submission Notes

Recommended DEV tags:

`devchallenge`, `gamechallenge`, `gamedev`

Recommended prize category:

Overall game jam prompt, with a light fit for `Best Ode to Alan Turing` because the obelisks are framed as a binary dawn cipher. Do not claim `Best Google AI Usage` unless real Google AI usage is added before submitting.

Verification artifacts:

- `verification-screenshot.png` for desktop rendering
- `verification-auto-demo.png` for auto-demo rendering
- `verification-recording-demo.png` for clean recording-mode rendering
- `verification-mobile.png` for narrow-screen rendering reference
